[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "derrom"
authors = [{name = "Stefan Meinecke", email = "stefan.meinecke@posteo.de"}]
dynamic = ["version", "description"]
